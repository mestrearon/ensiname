{{ include('IstEnsinameBundle:Grupo:form.html.twig', {
    'action': path('grupo_update', { 'id': entity.id }),
    'form': form,
    'legend': 'Редактировать профиль группы',
    'method': 'PUT'
}) }}
